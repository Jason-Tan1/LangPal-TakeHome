import React from 'react';
import { View, Text } from 'react-native';

export const ConversationPrompt = () => {
    return (
        <View>
            <Text>Conversation Prompt Component</Text>
        </View>
    );
};
