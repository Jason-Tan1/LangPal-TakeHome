import React from 'react';
import { View, Text } from 'react-native';

export const ResponseInput = () => {
    return (
        <View>
            <Text>Response Input Component</Text>
        </View>
    );
};
